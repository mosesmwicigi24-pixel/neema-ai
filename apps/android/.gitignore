.gradle/
build/
app/build/
local.properties
.idea/
*.iml
.kotlin/
captures/
*.keystore
!debug.keystore

# Test runs: Paparazzi's context cacheDir (the app's snapshot cache), not the screenshot goldens
app/snapshots/
