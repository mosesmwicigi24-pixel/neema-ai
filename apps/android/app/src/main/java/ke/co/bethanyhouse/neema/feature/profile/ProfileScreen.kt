package ke.co.bethanyhouse.neema.feature.profile

import androidx.compose.runtime.Composable
import ke.co.bethanyhouse.neema.app.DashboardViewModel
import ke.co.bethanyhouse.neema.core.ui.components.EmptyState

/** Placeholder — replaced by the full port of the web view. */
@Composable
fun ProfileScreen(dash: DashboardViewModel) {
    EmptyState(title = "Profile", subtitle = "Coming next")
}
