# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9     # Alembic sync driver
alembic==1.13.3

# Cache / Pub-Sub
redis==5.0.8

# Auth
pyjwt==2.9.0
passlib[bcrypt]==1.7.4

# Config & validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# HTTP (WABA calls)
httpx==0.27.2

# Utilities
python-multipart==0.0.12   # file uploads

# OpenAI API client
openai

# Anthropic API client (Tier 2 agent)
anthropic

# Async file handling (for media uploads/downloads)
aiofiles
# Phone parsing/normalization (country-aware E.164 for identity matching)
phonenumbers==8.13.45

# ── Call transcription (OPTIONAL — self-hosted, zero per-call cost) ──────────
# Uncomment to enable local Swahili/English call transcription on the box, then
# set WHISPER_ENABLED=1 (and WHISPER_AUTO=1 for automatic transcription). Ships
# manylinux wheels bundling ffmpeg (via PyAV) — no system packages needed. Left
# commented so it doesn't bloat the image until you turn transcription on.
# faster-whisper==1.0.3
